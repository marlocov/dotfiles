# dotfiles

## How to map the dotfiles with your home directory
```
sudo apt install stow
```

Use `stow` link the desired dotfile to the desired destination

```js
// Subject
~/dotfiles/nvim/.config/nvim

// Target
~/.config/nvim
```

